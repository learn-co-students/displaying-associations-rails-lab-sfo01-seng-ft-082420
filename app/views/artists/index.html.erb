<h1>Artists</h1>
  <% @artists.each do |artist| %>

      <%= link_to artist.name, artist_path(artist) %>
      <p>(<%= artist.song_count %> <%= "song".pluralize(artist.song_count) %>)</p>

  <% end %>
